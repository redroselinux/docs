# `terminus-font`

The `terminus-font` package includes Terminus consolefonts and std fonts. Unlike different distros,
in Redrose the consolefonts go in `/usr/share/consolefonts`, not `/usr/share/kbd/consolefonts`.
